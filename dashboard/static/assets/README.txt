Drop your brand files here (the dashboard auto-detects them):

  hati_logo.png    the square wolf logo  -> shown in the dashboard header
  hati_patch.png   the circular mission patch (optional)
  hati.ico         generated by dashboard/make_icon.py for the .exe build

If hati_logo.png is missing the header falls back to the built-in HATI wordmark.
